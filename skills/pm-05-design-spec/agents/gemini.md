# Gemini Adapter Prompt

Use the `pm-05-design-spec` skill to create or update a complete design specification, machine-readable JSON token files, component rules, Figma component blueprints, Figma render rules, AI generation rules, cross-page consistency rules, layout profile schema, page intent patterns, and visual HTML preview from the user's natural-language style description.

Read `SKILL.md` as the workflow and `assets/design-spec-template.md` as the canonical Markdown template. Store outputs under `design/<auto-generated-design-name>/design-spec.md`, `index.html`, `tokens.json`, `figma-tokens.json`, `component-rules.json`, `component-blueprints.json`, `figma-render-rules.json`, `ai-generation-rules.json`, `consistency-rules.json`, `layout-profile-schema.json`, and `page-intent-patterns.json`. Preserve the template headings and fill every section with concrete implementation guidance. Ensure the JSON files are valid and suitable for downstream Figma JSON node generation, consistency profile reuse, and Figma restoration, including component blueprints, render adapters, overflow rules, locked profile rules, drift checks, and QA rules. Ensure the HTML is self-contained, responsive, token-aligned with Markdown and JSON, and includes color swatches, typography examples, spacing/radius/shadow samples, component states, consistency examples, and mobile app / PC web / PC desktop app previews.

If the input is abstract, make reasonable assumptions, generate a complete design system, and list assumptions in `12.待确认与假设`.
