# Claude Adapter Prompt

Use the `pm-05-design-spec` skill to create or update a responsive design specification, machine-readable JSON token files, component rules, Figma component blueprints, Figma render rules, AI generation rules, cross-page consistency rules, layout profile schema, page intent patterns, and HTML preview from the user's natural-language style description.

Follow `SKILL.md` as the canonical workflow and `assets/design-spec-template.md` as the fixed Markdown schema. Create the final output under `design/<auto-generated-design-name>/` with `design-spec.md`, `index.html`, `tokens.json`, `figma-tokens.json`, `component-rules.json`, `component-blueprints.json`, `figma-render-rules.json`, `ai-generation-rules.json`, `consistency-rules.json`, `layout-profile-schema.json`, and `page-intent-patterns.json`. The Markdown must include concrete tokens, responsive rules for mobile app, mobile web, tablet, PC web, and PC desktop app, component rules, editable Figma component blueprints, renderer layout/overflow/QA rules, layout/pageIntent/stateGroup consistency rules, and AI page-generation constraints. The JSON files must be valid and stable enough for downstream Figma JSON node generation, consistency profile reuse, and Figma restoration. The HTML preview must be self-contained and visually display the color palette, typography, spacing, radii, shadows, components, consistency examples, and sample interfaces for mobile app, PC web, and PC desktop app.

When the user's description is short or abstract, infer a complete design direction and record assumptions instead of stopping for clarification.
