# PM 10 Figma Checkout Adapter

Follow `SKILL.md` exactly. Keep target resolution, issue detection, repair plan, Figma write operations, and post-fix QA deterministic. A canvas must be selected before repair. Before Figma inspection, read `product/Product Overview.md` and `product/layout/*.md` and build `ProductLayoutModel`, `ReusableElementCatalog`, and `stateVariantClusters`. When no frame/component/node is specified, treat the scope as all eligible elements in the selected canvas. Always check frame relationships, all reusable element clusters from sitemap, component family structure/style/token consistency, same-state shared region consistency, navigation active states, absolute layout overuse, dynamic FILL/HUG sizing, and redundant generated content. Prefer Figma metadata/design context/screenshots before any repair.
