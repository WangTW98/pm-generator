# PM 10 Figma Checkout Adapter

Use `SKILL.md` as the source of truth. A target canvas is required; if the user does not specify one, stop and ask which canvas to use. Before Figma inspection, read `product/Product Overview.md` and `product/layout/*.md`, build `ProductLayoutModel`, `ReusableElementCatalog`, and `stateVariantClusters` from sitemap pages, regions, elements, and state groups. If the user specifies a canvas but no frame/component/node, inspect and safely repair all eligible elements in that canvas. Include cross-frame relationship checks, full reusable element cluster coverage, component family structure/style/token consistency, same-state shared region consistency, correct navigation/Tab active state, absolute layout overuse, dynamic FILL/HUG sizing validity, and redundant generated content removal from visible page scope. Prioritize Figma remote MCP reads, then `use_figma` for structural checks and safe fixes. Do not modify Figma in `check-only` mode. Always produce a checkout report.
