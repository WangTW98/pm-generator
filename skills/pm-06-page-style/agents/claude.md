# Claude Adapter Prompt

Use the `pm-06-page-style` skill to generate a default-state-only, render-ready Figma JSON node document for exactly one `product/pages/**/layout.md` file.

Follow `SKILL.md` as the canonical workflow. Use `assets/figma-nodes-template.json` as the JSON schema reference and `assets/figma-node-notes-template.md` as the notes schema. Resolve the design spec from the user's specified `design/<name>/` directory or auto-match a valid design spec under `design/`. Read the source page's `来源Sitemap`, `使用Layout`, `页面清单ID`, `1.3.1.默认状态页面结构`, default-state elements, and default-state mock data, then read the source sitemap to merge the global layout shell. Exclude empty/loading/error/success/modal-open/drawer-open state variants from generated Figma nodes and record them only as excluded states or notes. Use `tokens.json`, `figma-tokens.json`, `component-rules.json`, `component-blueprints.json`, `figma-render-rules.json`, `ai-generation-rules.json`, `consistency-rules.json`, `layout-profile-schema.json`, and `page-intent-patterns.json` to write `figma-nodes.json` and `figma-node-notes.md` next to the source `layout.md`.

Before generating nodes, classify `pageIntent` using generic intent values, apply the matching semantic pattern from the design spec, and write `semanticValidation`. If the page structure conflicts with the intent, repair from stronger source evidence or fail the page; do not emit schema-valid but semantically wrong JSON.

Generate valid JSON only for `figma-nodes.json`. Include `renderSpec` with component adapters, constraints, text policies, layout safety, layout budget, slot contracts, duplicate audit, and QA expectations so `pm-08` does not infer component structure from prose. Include semantic ownership, duplicate-action, and layout-budget audits in `semanticValidation`. Include top-level `consistency` with layoutKey, pageIntentKey, stateGroupKey, designSpecKey, profileRefs, styleFingerprint, lockedPropertiesApplied, and driftCheck so same-layout and same-state pages remain stable. Keep node names stable, reference tokens where possible, and record assumptions, semantic conflicts, consistency profile decisions, and repairs in the notes file.
