# Claude Adapter Prompt

Use the `pm-03-page-layout` skill to create or update exactly one page layout document from a specified `product/layout/<应用端>-sitemap.md` and one page-list ID. If the user instruction contains test, dry-run, preview, or directory-only intent, compute the canonical path, create the canonical parent directory, and create an empty `.md` placeholder file when absent without generating page layout content.

Follow `SKILL.md` for lifecycle and merge rules. Use `assets/page-layout-template.md` as the fixed Markdown schema. Keep headings, headerless status HTML table, layout reference table, default-state-first page content structure, default-state structure diagram, element table, mock data tables, assumption-list labels, and ID formats exactly consistent. Treat direct user edits in sections `1` and `2` as approved content unless superseded by newer instructions. Record `来源Sitemap` and `使用Layout`, but do not inline global layout/sitemap shell elements. Generate `1.4.默认状态页面结构图` as a Mermaid `flowchart TB` from `1.3.1.默认状态页面结构`; do not treat it as a synchronized mirror of `1.5.页面元素清单`. Do not create `元素来源 = Sitemap Layout` rows; page element rows must describe only the current page body using `Page Content`, `Derived State`, or `Mock Data`.

When the selected sitemap row has `页面类型 = 节点`, stop without creating or updating a page layout document and ask for a child `页面` or `状态` row. Always read page IDs, parent IDs, types, names, roles, sources, and state groups from `2.2.页面清单`, and always compute the output directory from the page-list parent chain. If the sitemap instruction or review notes indicate `思维导图优先`, first validate that `2.2.页面清单` fully mirrors `2.1.sitemap思维导图`; every mind-map item must have a `PAGE-xxx` row and the list `父级ID` / `层级` / `页面类型` must reconstruct the same tree. Do not create text-only path segments and never insert `<STATE-ID>-<状态组名称>` folders from `状态组`. If validation fails, stop and ask the user to sync the page list with `pm-02-sitemap`. For all rows, including rows with `状态组`, include the selected row's own `<PAGE-ID>-<页面/模块>` segment as the final directory segment. Use `状态组` only for baseline reuse, state consistency, and metadata; it must not alter paths. Reuse the best baseline: highest-version Release first, otherwise latest/highest-version Development, changing only current-state differences. Generate only the selected eligible PAGE document under `product/pages/<来源Sitemap Layout>/<完整父子目录链>/`. Before writing, scan for legacy files with the same `页面清单ID` and migrate exactly one legacy file to the canonical target path when the canonical file does not already exist. Do not generate sibling state pages or batch outputs.

For `DryRun` triggers such as `测试`, `dry run`, `预演`, `试运行`, or `不生成文档`, and `DirectoryOnly` triggers such as `只生成目录结构`, `仅生成目录结构`, `只生成目录`, or `仅创建目录`, validate the row, compute the canonical output path, create the canonical parent directory, and create an empty zero-byte `.md` placeholder only if the canonical target file does not already exist. Preserve existing target files unchanged. Report the selected row, canonical output path, parent directory, placeholder status, state group, same-state baseline candidate, and legacy-file status.

If the user requests a formal/release/正式版 document, set `文档类型` to `Release` and remove `## 3.待确认与假设` and `## 4.用户补充说明` completely before returning the document.
